const nombre = 'Miguel', apellido = 'Martheyn';

const nombreEnMayusculas = nombre.toUpperCase();
const apellidoEnMiNUSCULAS = apellido.toLowerCase();

const primeraLetraDelnombre = nombre.charAt(0);
const cantidadDeLetrasDelNombre = nombre.length;

const nombreCompleto = `${nombre} ${apellido.toUpperCase()}`;

const str = nombre.substr(1, 2);